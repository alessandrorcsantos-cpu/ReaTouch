# Ícones

O ícone do menu usa o símbolo SF `slider.horizontal.3`, que acompanha o macOS Ventura e se adapta automaticamente ao modo claro/escuro. Adicione um `AppIcon.appiconset` em `Assets.xcassets` se desejar um ícone próprio para o pacote `.app`.
